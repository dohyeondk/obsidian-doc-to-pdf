[project]
name = "obsidian-doc-to-pdf"
version = "0.1.0"
description = "Download Obsidian Help documentation as a single PDF with table of contents"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "playwright>=1.40.0",
    "pypdf>=5.1.0",
]
